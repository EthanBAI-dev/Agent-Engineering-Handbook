# F24-1｜校验夹在规划和执行之间

- 课：第 24 课 · Planner–Executor：先把计划写下来，才能检查它
- 形式：流程图 · `assets/lessons/24-plan-validate-execute.svg`
- 目标文件：`assets/lessons/24-plan-validate-execute.svg`

## 路线 A（推荐）· 让能写代码的模型直接产出 SVG

把下面整段贴给 Claude / Codex：

```text
【任务】写一个 SVG，落到 apps/web/public/assets/lessons/24-plan-validate-execute.svg

【这张图在讲什么】
## 重规划次数必须有上限

如果那个失败的步骤永远失败，而 planner 每次都坚持要它：

    重规划 2 次后：放弃：步骤 parse 失败，已完成 ['fetch', 'fetch', 'fetch']
    上限 MAX_REPLANS = 2

停住了，而且给出了一个说得通的结果。

没有上限的话，这就是第 10 课那个停不下来的循环换了个形式——而且每次重规划都要调一次模型，烧钱更快。

注意 `fetch` 执行了三次。这也再次说明上面那条：重做是必然的，幂等是必须的。

【画什么】
`planner → validate → executor`，validate 下方一条岔路通向 give_up。 一份含 `rm_rf` 的计划卡片在 validate 处被挡下，executor 一侧标「一步都没执行」。

【必须标注】
校验发生在执行之前；重规划的回边要标上限次数。

【不要出现】
把 validate 画成可选步骤——它是这个模式唯一的安全收益。

【替代文本（原样写进 <title> 与 <desc>）】
计划先经过校验再交给执行器，含非法步骤的计划在执行前就被拦下。

【硬约束】
  - viewBox="0 0 720 H"，width="100%" height="auto"，不写固定 px 宽度
  - 颜色写成带兜底值的 CSS 变量：
      --ink #17231f  --muted #68736d  --card #fffdf7  --line #c9cfc7
      --lime #bafa4b（配 --on-lime #4d7100）  --green #355e48  --tertiary #9c432c
  - 圆角 rx="8"（小徽章 rx="2"）
  - 不用滤镜／渐变／blur；要立体感就用 2px 3px 的实心偏移矩形
  - font-family="var(--font-ui)"，正文 13px、标签 11px、标题 15px
  - 箭头用统一的 <marker>
  - 中文直接写在 <text> 里，不要转曲线

【自检】
  - 「必须标注」的每一条都能在 <text> 里找到
  - 「不要出现」的每一条确实没有出现
```

## 路线 B · Stitch（仅当这张图本身就是界面示意）

先粘 `../00-style-contract.md` 的契约文本，再粘：

```text
【任务】
画一张界面示意图，它会作为插图嵌在文章里，不是一个可用的页面。

【画什么】
`planner → validate → executor`，validate 下方一条岔路通向 give_up。 一份含 `rm_rf` 的计划卡片在 validate 处被挡下，executor 一侧标「一步都没执行」。

【必须能看清这些文字】
校验发生在执行之前；重规划的回边要标上限次数。

【不要出现】
把 validate 画成可选步骤——它是这个模式唯一的安全收益。

【尺寸】
宽 920px，高度自适应；四周内边距 24px；底色 #fffdf7，外面一圈 1px #c9cfc7 描边、8px 圆角。
不要标题栏，不要浏览器窗口装饰。
```
