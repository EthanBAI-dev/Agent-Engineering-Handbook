# F02-1｜谁决定，谁执行

- 课：第 02 课 · 模型不会亲手算：它先决定要不要调用工具
- 形式：职责对照图 · `assets/lessons/02-who-does-what.svg`
- 目标文件：`assets/lessons/02-who-does-what.svg`

## 路线 A（推荐）· 让能写代码的模型直接产出 SVG

把下面整段贴给 Claude / Codex：

```text
【任务】写一个 SVG，落到 apps/web/public/assets/lessons/02-who-does-what.svg

【这张图在讲什么】
## 从第一课加两个节点

第一课的图由 `plan`、`work` 和 `review` 组成，路线由代码提前写好。

现在把中间换成两个新角色：

    START → model ──有 tool call──→ tools
              ↑                       │
              └──── tool result ──────┘
              │
              └──没有 tool call──→ END

图中还出现了 `START`（图入口）、`END`（图出口）和 tool result（工具返回结果）。

`START` 启动流程；`END` 结束流程；tool result 把工具执行结果送回 model。

State（运行状态）、Node（节点）和 Edge 没有消失。我们只是把 State 换成消息列表，把节点换成模型与工具。

State 保存当前消息现场；Node 完成一次模型或工具操作。

这也是为什么不能跳过第一课：ReAct（推理—行动循环）仍然是一张图。

ReAct 让模型反复经历“判断下一步—执行工具—观察结果”，直到能够给出最终回答。

【画什么】
左右两栏。左栏 model：一张写着 `tool: add / args: {a:128, b:349}` 的便签； 右栏 tools：一个真的在跑的函数框，输出 `477`。中间一条箭头从左到右，另一条从右回到左。

【必须标注】
左栏「只写下想调用什么」，右栏「真的执行 Python 函数」；回边标 `tool result`。

【不要出现】
model 里画计算器或算式——它不算，这正是本课要纠正的。

【替代文本（原样写进 <title> 与 <desc>）】
model 生成写明工具名和参数的调用请求，tools 执行真实函数并把结果送回 model。

【硬约束】
  - viewBox="0 0 720 H"，width="100%" height="auto"，不写固定 px 宽度
  - 颜色写成带兜底值的 CSS 变量：
      --ink #17231f  --muted #68736d  --card #fffdf7  --line #c9cfc7
      --lime #bafa4b（配 --on-lime #4d7100）  --green #355e48  --tertiary #9c432c
  - 圆角 rx="8"（小徽章 rx="2"）
  - 不用滤镜／渐变／blur；要立体感就用 2px 3px 的实心偏移矩形
  - font-family="var(--font-ui)"，正文 13px、标签 11px、标题 15px
  - 箭头用统一的 <marker>
  - 中文直接写在 <text> 里，不要转曲线

【自检】
  - 「必须标注」的每一条都能在 <text> 里找到
  - 「不要出现」的每一条确实没有出现
```

## 路线 B · Stitch（仅当这张图本身就是界面示意）

先粘 `../00-style-contract.md` 的契约文本，再粘：

```text
【任务】
画一张界面示意图，它会作为插图嵌在文章里，不是一个可用的页面。

【画什么】
左右两栏。左栏 model：一张写着 `tool: add / args: {a:128, b:349}` 的便签； 右栏 tools：一个真的在跑的函数框，输出 `477`。中间一条箭头从左到右，另一条从右回到左。

【必须能看清这些文字】
左栏「只写下想调用什么」，右栏「真的执行 Python 函数」；回边标 `tool result`。

【不要出现】
model 里画计算器或算式——它不算，这正是本课要纠正的。

【尺寸】
宽 920px，高度自适应；四周内边距 24px；底色 #fffdf7，外面一圈 1px #c9cfc7 描边、8px 圆角。
不要标题栏，不要浏览器窗口装饰。
```
