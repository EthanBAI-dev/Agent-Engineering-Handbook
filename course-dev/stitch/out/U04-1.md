# U04-1｜批准一条，拒绝一条

- 课：第 04 课 · Agent（智能体）怎样暂停，等人类批准后再继续
- 状态：已实现 · `apps/web/src/components/lesson-four-lab.tsx`
- 骨架：`ScenarioLab`（`apps/web/src/components/lab-kit.tsx`）

> ⚠️ 这个实验**已经实现**（已实现 · `apps/web/src/components/lesson-four-lab.tsx`）。
> 下面的提示词只用于**换皮**：让 Stitch 出状态图，再把版式改进 `globals.css` 的 `.kit-*` 规则，
> **不要**据此重写 `lab-kit.tsx` 的 DOM 结构——31 课的实验都挂在那套类名上，
> 改结构等于同时改 31 课，Playwright 的验证脚本也是按这些类名找元素的。

## 可直接粘贴（契约 + 任务，已拼好；记得附 `../reference/` 里的截图）

```text
【风格契约 · 必须严格遵守，附图为准】

这是一个已经上线的中文技术教学网站的新页面，不是重新设计。
附图是现有页面的真实截图，你的产出必须能和它并排放在同一个网站里而看不出差别。

配色（只用这些十六进制值，不要自创）
  正文墨色  #17231f      次要文字  #68736d
  页面底色  #f2efe6      卡片底色  #fffdf7
  描边      #c9cfc7      浅描边    #e2ded5
  强调亮色  #bafa4b（柠檬绿，只用于「当前 / 已选中 / 通过」，底上的字用 #4d7100）
  深绿      #355e48      成功浅底  #e8efe9
  警示珊瑚  #ff8f72      警示深字  #9c432c   警示浅底 #ffe0d8
  浅底阶梯  #effdf6 → #e9f7f0 → #e3f1ea → #ddebe5 → #d8e6df
  终端黑    #13221b（代码块底），终端上的字用 #e6f4ed

字体
  界面文字与标题：Inter（无衬线）
  长段落正文：衬线（Latin 用 Newsreader 一类，中文用思源宋体一类）
  代码、数字、状态值：等宽（JetBrains Mono 一类）
  正文 16px / 行高 1.85；小字 13px；标题 clamp(26px, 3.4vw, 40px)

圆角（克制，不要圆滚滚的气泡）
  徽章 2px · 按钮 4px · 卡片与面板 8px · 主 CTA 12px · 只有状态圆点是全圆

投影（硬投影，不要弥散的柔光阴影，不要 blur）
  卡片      0 2px 0 0 #c9cfc7
  可交互块  2px 3px 0 0 #17231f
  运行中    3px 4px 0 0 #355e48
  浮层      4px 6px 0 0 rgba(23,35,31,.25)

版心
  正文与几乎所有区块共用一条 710px 的主栏，居中。
  只有互动实验区块可以加宽到 920px，这是全页唯一一次刻意的破格。
  不要出现「一会宽一会窄」的段落：除互动实验外，所有区块左右边缘必须对齐。

绝对不要出现
  ✗ 紫色到蓝色的渐变、玻璃拟态、霓虹光晕
  ✗ 圆形头像、星级评分、价格卡、"Get Started Free" 一类 SaaS 落地页套路
  ✗ emoji 图标；需要图标时用极细线条的几何图形
  ✗ 弥散阴影（box-shadow 带 blur）
  ✗ 英文占位文案 Lorem ipsum；所有文字用我给你的中文原文

【任务】
为第 04 课的互动实验「批准一条，拒绝一条」出**状态图**，不要做交互。
同一个组件画下面几个状态，纵向排列，每个状态上方标一行小字说明它是什么状态：
  ① 初始态  ② 改了控件但还没达成  ③ 达成态  ④ 结果里出现失败行（珊瑚色）

【这个实验在教什么】
p.py`](../../lab/langgraph/examples/04_human_in_loop.py)。

它不调用模型，也不执行真实删除，所以不需要 API Key（接口密钥）。在 `lab/langgraph` 目录运行：

API Key 用来验证程序是否有权调用模型服务；本课没有模型调用，所以不需要配置。

    uv run python examples/04_human_in_loop.py

当前脚本已经在本机实跑通过。你会看到：

    /tmp/cache 收到 yes → 已删除 /tmp/cache
    /etc/passwd 收到 no → 已取消

“已删除”只是 State 中的演示文本，不代表脚本调用了文件系统删除命令。

【用户做什么】
选一条操作和副作用位置，调用图；图暂停后点批准或拒绝。

【屏幕上变什么】
approval 变色挂 ⏸；State 面板显示 `next` 元组；载荷面板显示 payload； 四个计数器实时更新（propose / interrupt 前 / interrupt 后 / 副作用实际发生）。

【通过条件】
两个 thread 分别得到「已删除 /tmp/cache」和「已取消」。

【小屏怎么排】
选择区与面板纵向堆叠，计数器单列，流程条横向滚动。

【组件结构 · 从上到下】
1. 工具条：左边等宽小字「INTERACTIVE 04」+ 下面一行组件标题；
   右边一枚状态药丸（未达成：浅底描边；达成：#bafa4b 实底 + #4d7100 字）
2. 控件区：左侧标签列 + 右侧控件列；720px 以下改上下堆叠
   分段按钮：并排方角按钮（4px 圆角），选中项 #bafa4b 实底 + 2px 3px 0 0 硬投影
   开关：方形复选框 + 一行说明文字，包在描边小卡里，打开时描边变 #355e48
   滑杆：细轨道 + 方形滑块，右侧一个等宽数值
3. 结果区：左右等宽两栏，720px 以下改上下堆叠
   左栏是定义列表，每行「标签 — 值」：标签 13px 次要色，值等宽加粗 13px 右对齐，
   行间虚线分隔，值有绿／珊瑚／灰三种色调；**标签不许被挤成竖排单字**
   右栏是 1–3 段说明，好的一段浅绿底圆角块，坏的一段珊瑚浅底圆角块，中性的是纯文字
9. 判定条：整宽一条，未达成时浅底 + 次要色文字；达成时 #bafa4b 实底 + 深色文字 + 末尾 ✓
10. 脚注：12px 次要色一行，说明数据来自真实运行
11. 折叠的「展开代码」，收起时只是一行带三角的小标题

【硬约束】
  - 这个区块是全页唯一允许加宽到 920px 的部分，正文其余部分是 710px
  - 任何宽度下都不许出现整页横向滚动
  - 不要画鼠标指针、不要画「点击这里」的箭头引导

【输出】
桌面 1440px 和移动 390px 各一版。
```
