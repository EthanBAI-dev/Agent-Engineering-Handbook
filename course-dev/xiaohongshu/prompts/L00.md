# 第 00 课 · 小红书图文

## 标题（上限 20 字，第一个是主推）

1. 搭AI Agent第一步不是提示词　　（17 字）
2. Agent必备技术栈：LangGraph　　（20 字）
3. 0基础搭Agent｜环境10分钟　　（16 字）

## Stitch 提示词（整段复制，附 `../out/L00/01.png` 当参考图）

```text
【风格契约 · 必须严格遵守，附图为准】

这是一个已上线的中文技术教学账号的图文卡片，不是重新设计品牌。
附图是同一套视觉的真实成品，你的产出必须能和它放在同一个账号里而看不出差别。

配色（只用这些十六进制值，不要自创）
  正文墨色  #17231f      次要文字  #68736d
  页面底色  #f2efe6      卡片底色  #fffdf7
  描边      #c9cfc7      浅描边    #e2ded5
  强调亮色  #bafa4b（柠檬绿，只用于「当前 / 已选中 / 通过」，底上的字用 #4d7100）
  深绿      #355e48      成功浅底  #e8efe9
  警示珊瑚  #ff8f72      警示深字  #9c432c   警示浅底 #ffe0d8
  浅底阶梯  #effdf6 → #e9f7f0 → #e3f1ea → #ddebe5 → #d8e6df
  终端黑    #13221b（代码块底），终端上的字用 #e6f4ed

圆角（克制，不要圆滚滚的气泡）
  徽章 2px · 按钮 4px · 卡片与面板 8px · 主 CTA 12px · 只有状态圆点是全圆

投影（硬投影，不要弥散的柔光阴影，不要 blur）
  卡片      0 2px 0 0 #c9cfc7
  可交互块  2px 3px 0 0 #17231f
  运行中    3px 4px 0 0 #355e48
  浮层      4px 6px 0 0 rgba(23,35,31,.25)

绝对不要出现
  ✗ 紫色到蓝色的渐变、玻璃拟态、霓虹光晕
  ✗ 圆形头像、星级评分、价格卡、"Get Started Free" 一类 SaaS 落地页套路
  ✗ emoji 图标；需要图标时用极细线条的几何图形
  ✗ 弥散阴影（box-shadow 带 blur）
  ✗ 英文占位文案 Lorem ipsum；所有文字用我给你的中文原文

【任务】
做一组小红书图文卡片，共 8 张，纵向排列在同一页里。
每张卡片**严格 621 × 828 CSS px**（3:4），给每张卡加 class="xhs-card"，
卡与卡之间留 24px 间距。我会用脚本按这个类名逐张截图导出成 1242×1656。

【最重要的一条】
下面每张卡的文字都是最终稿。你的工作是排版，不是写作：
不许改写、精简、总结、换措辞，不许省略任何一句，不许加英文占位文案。
排不下就调字号和行距，不许删字。

【小红书的版式要求】
  - 手机上看得清：封面主标题 ≥ 64px，内页小标题 ≥ 38px，正文 ≥ 24px，行高 1.75
  - 一张卡只讲一件事，正文控制在 3–5 行，宁可留白也不要塞满
  - 每张卡四周内边距 52px，外面再套一圈 10px 的米白色内框
  - 卡片底纹：22px 网格的极淡圆点，只出现在内页，封面和尾页是纯色
  - 不要页码以外的装饰元素，不要 emoji，不要圆形头像

【卡片逐张脚本】
【第 1 张 · 封面】
  主标题（三行，逐字使用）：搭 AI Agent ／ 第一步不是 ／ 写提示词
  其中「写提示词」要用柠檬绿高亮块包住
  副标题：LangGraph 技术栈 · 从 0 到能跑
  左上角一枚黑底柠檬绿字的方形徽章：LESSON 00 · 入门

【第 2 张】
  小标题：先分清两件事
  正文（逐字使用，** 之间的字要用柠檬绿下划高亮）：
    模型负责「想」，编排器负责「流程」。
    
    提示词写得再好，也管不了「失败了要重试几次」「这一步要不要等人批准」。那是编排器的活。
  底部一条浅绿色强调条：LangGraph 就是编排器

【第 3 张】
  小标题：LangGraph 到底是什么
  正文（逐字使用，** 之间的字要用柠檬绿下划高亮）：
    不是模型，不是插件，不是提示词框架。
    
    它是一张**状态图**：你把流程画成 State、Node、Edge，它负责按图执行、保存现场、在中途停下来。
  代码块（深色底 #13221b，等宽字，不要行号）：
    State  运行状态
    Node   一步工作
    Edge   下一步去哪

【第 4 张】
  小标题：30 课最后做出什么
  正文（逐字使用，** 之间的字要用柠檬绿下划高亮）：
    一个能循环、有记忆、危险操作前会停下来等你批准的 Agent。
    
    不是 demo，是能说清「重启之后还在不在」「重复提交会不会执行两次」的东西。

【第 5 张】
  小标题：三个工具先装好
  正文（逐字使用，** 之间的字要用柠檬绿下划高亮）：
    Python 3.10+、uv、一个终端。
    
    依赖不用一个个装，`uv sync` 一行搞定，它会自己建虚拟环境。
  代码块（深色底 #13221b，等宽字，不要行号）：
    python --version
    uv --version
    uv sync

【第 6 张】
  小标题：前 6 课不需要 API Key
  正文（逐字使用，** 之间的字要用柠檬绿下划高亮）：
    课程里用一个脚本化的假模型，跑的是**真实的图结构**。
    
    学的是编排，不是烧 token。等你真的需要模型了再接。
  底部一条浅绿色强调条：省钱且可复现

【第 7 张】
  小标题：Key 什么时候才需要
  正文（逐字使用，** 之间的字要用柠檬绿下划高亮）：
    第 07 课。而且只放服务端环境变量。
    
    绝对不要进浏览器代码，不要放 `NEXT_PUBLIC_` 开头的变量里——那等于公开发布。
  小标题前加一枚珊瑚色小标签「易踩坑」

【第 8 张 · 尾页】
  标题：三句话带走
  三条编号列表（柠檬绿方形数字块）：
    模型负责想，LangGraph 负责流程
    前 6 课不用 API Key 也能学完
    Key 只放服务端，永远不进前端
  底部一块墨色卡片：全 31 课 · 每课都有可以自己拨的互动实验／主页合集持续更新

【输出】
一页 HTML，包含全部 8 张卡片，每张 class="xhs-card"。
```

## 正文文案

```text
很多人搭 Agent 卡在第一步：以为是提示词的事。

其实模型只负责「想」，「流程」得有人管——重试几次、要不要等人批准、断了从哪接上，这些提示词写破天也管不了。这就是 LangGraph 干的事。

这条是第 00 课，讲清楚技术栈和环境。前 6 课全程不需要 API Key，用脚本化假模型跑真实图结构，学编排不烧 token。

后面会一课一课更完，从 State/Node/Edge 一直到人工审批、长期记忆和安全边界。

#AIAgent #LangGraph #大模型应用开发 #程序员 #AI学习
```

## 配图（如果想额外配一张插画）

```text
一台老式打字机和一张折叠的流程蓝图并排放在米白色桌面上，蓝图上是简单的方框和箭头

风格：扁平矢量插画，粗细均匀的线条，柠檬绿 #bafa4b 与深绿 #355e48 点缀，
米白 #f2efe6 底色，硬边不要渐变，不要光晕，不要 3D，不要写实材质。
画面里不要出现任何文字。
比例 3:4。
```

## 本地已渲染

`out/L00/` 里有 8 张 1242×1656 的成品，可以直接发；
`preview.html` 是同一份版式，可以拖进 Stitch 当参考。
